

function checkPeers(){
/*console.log("run");
for (i=0;i<numberOfStudents;i++)
	peerPool[i] = [];


for (i=0;i<numberOfStudents;i++){
	for(j=0;j<numberOfStudents;j++){
		for(k=0;k<5;k++){
			if(assigned[j][k] == i)
			{
				for (l=0;l<5;l++){
				if (peerPool[i].indexOf(assigned[j][l])==-1 && i!=assigned[j][l])  //if not already available
					peerPool[i].push(assigned[j][l]);
				
				}
				
				
			}

		}
	}
}

*/

peerPool = peers;
//console.log(peers);



}